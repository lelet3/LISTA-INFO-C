#include <stdio.h>
#include <string.h>

int main () {
    char nome [40];
    
    printf("Digite o seu nome: ");
    fgets(nome, 40, stdin);
    for (int i = 0; i < 4; ++i) 
    {
        printf("%c", nome [i]);
    }
    
    return 0;
}