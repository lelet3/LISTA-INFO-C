#include <stdio.h>
int main() {
    int num, antec, suc;

    printf("Digite um numero inteiro: ");
    scanf("%d", &num);

    antec = num - 1;
    suc = num + 1;

    printf("Antecessor: %d\n", antec);
    printf("Sucessor: %d\n", suc);

    return 0;
}
